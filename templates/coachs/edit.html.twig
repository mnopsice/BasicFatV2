<!DOCTYPE html>

<title>Edit Coachs</title>

{% block body %}
    <h1>Edit Coachs</h1>

    {{ include('coachs/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_coachs_index') }}">back to list</a>

    {{ include('coachs/_delete_form.html.twig') }}
{% endblock %}
