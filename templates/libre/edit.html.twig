<!DOCTYPE html>

<title>Edit Libre</title>

{% block body %}
    <h1>Edit Libre</h1>

    {{ include('libre/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_libre_index') }}">back to list</a>

    {{ include('libre/_delete_form.html.twig') }}
{% endblock %}
