<form method="post" action="{{ path('app_libre_delete', {'id': libre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ libre.id) }}">
    <button class="btn">Delete</button>
</form>
