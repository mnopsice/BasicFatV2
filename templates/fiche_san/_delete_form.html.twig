<form method="post" action="{{ path('app_fiche_san_delete', {'id': fiche_san.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_san.id) }}">
    <button class="btn">Delete</button>
</form>
