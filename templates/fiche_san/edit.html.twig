<!DOCTYPE html>

<title>Edit FicheSan</title>

{% block body %}
    <h1>Edit FicheSan</h1>

    {{ include('fiche_san/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_fiche_san_index') }}">back to list</a>

    {{ include('fiche_san/_delete_form.html.twig') }}
{% endblock %}
